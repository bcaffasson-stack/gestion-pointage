# Configuration backend (optionnel)
# Le frontend utilise par defaut des chemins relatifs /api (proxifies par Vercel vers le backend Render).
# Cette variable n'est utile que si le frontend est heberge ailleurs sans proxy.
# Nom : VITE_API_URL
VITE_API_URL=